// script.js

// Add any custom JavaScript functionality here in the future.
// For example, you might add:
// - Smooth scrolling for anchor links (if CSS method isn't sufficient)
// - Image lightbox functionality
// - Interactive map elements (though Google Maps links handle basic maps)
// - Mobile menu toggle

console.log("JavaScript file loaded."); // Optional: confirms the file is linked correctly

// Example: Smooth scrolling for anchor links (alternative to CSS)
/*
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();

        const targetId = this.getAttribute('href');
        const targetElement = document.querySelector(targetId);

        if (targetElement) {
            targetElement.scrollIntoView({
                behavior: 'smooth',
                block: 'start' // Adjust as needed (e.g., 'center')
            });
        }
    });
});
*/
